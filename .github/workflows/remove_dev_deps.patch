diff --git a/base/Cargo.toml b/base/Cargo.toml
index fc88cee..47d5459 100644
--- a/base/Cargo.toml
+++ b/base/Cargo.toml
@@ -22,7 +22,6 @@ std = []

 [dev-dependencies]
 rand = { version = "0.8.3" }
-criterion = { version = "0.5.1", features = ["html_reports"] }

 [[bench]]
 name = "benchmarks"
diff --git a/float/Cargo.toml b/float/Cargo.toml
index afc2141..c633147 100644
--- a/float/Cargo.toml
+++ b/float/Cargo.toml
@@ -59,9 +59,6 @@ rand_v08 = { version = "0.8.3", package = "rand" }
 postcard = { version = "1.0.2", features = ["alloc"] }
 serde_test = { version = "1.0.130" }
 serde_json = { version = "1.0" }
-postgres = { version = "0.19.4" }
-
-criterion = { version = "0.5.1", features = ["html_reports"] }

 [[test]]
 name = "random"
diff --git a/integer/Cargo.toml b/integer/Cargo.toml
--- a/integer/Cargo.toml
+++ b/integer/Cargo.toml
@@ -25,12 +25,6 @@ rand = ["rand_v08"]
 num-traits = ["num-traits_v02"]
 num-integer = ["num-integer_v01"]

-# Enables the hegel-based stateful equivalence tests in tests/stateful_*.rs.
-# Required-features on those [[test]] entries below ensures `cargo test`
-# without this flag will not compile them. Also enables `rand` so the tests
-# can build seeded RNGs.
-hegel-tests = ["rand"]
-
 [dependencies]
 dashu-base = { version = "0.4.0", default-features = false, path = "../base" }
 cfg-if = { version = "1.0.0" }
@@ -54,15 +48,6 @@ postcard = { version = "1.0.2", features = ["alloc"] }
 serde_test = { version = "1.0.130" }
 serde_json = { version = "1.0" }

-criterion = { version = "0.5.1", features = ["html_reports"] }
-
-# Stateful equivalence tests against num-bigint. Gated behind the `hegel-tests`
-# feature so the default `cargo test` doesn't pull these in on toolchains that
-# can't compile hegeltest (it requires edition 2024 / rustc >= 1.86).
-hegeltest = { version = "0.14", features = ["native"] }
-num-bigint = "0.4"
-num-traits = "0.2"
-
 [lib]
 bench = false

@@ -78,25 +63,7 @@ required-features = ["serde"]
 name = "num_order"
 required-features = ["num-order"]

-[[test]]
-name = "stateful_ibig"
-required-features = ["hegel-tests"]
-
-[[test]]
-name = "stateful_ubig"
-required-features = ["hegel-tests"]
-
 [[bench]]
 name = "primitive"
 required-features = ["rand"]
 harness = false
-
-[[bench]]
-name = "small_int"
-required-features = ["rand"]
-harness = false
-
-[[bench]]
-name = "workload"
-required-features = ["rand"]
-harness = false
diff --git a/rational/Cargo.toml b/rational/Cargo.toml
index 93a7c96..772b479 100644
--- a/rational/Cargo.toml
+++ b/rational/Cargo.toml
@@ -53,8 +53,6 @@ postcard = { version = "1.0.2", features = ["alloc"] }
 serde_test = { version = "1.0.130" }
 serde_json = { version = "1.0" }

-criterion = { version = "0.5.1", features = ["html_reports"] }
-
 [lib]
 bench = false

